@inherits PageListView<FormModel>

<MxView>
	<Actions>
		&nbsp;
		<OptionMenu>
			<Items>
				<OptionItem Text="Add" OnClick="() => Form()" />
			</Items>			
		</OptionMenu>
	</Actions>
	<Body>
		<TelerikListView @ref="list"
						 TItem="FormModel"
						 OnRead="ReadItemsAsync"
						 Pageable="true"
						 PageSize="Source.PageSize">
			<Template>
				<div class="d-flex align-items-start">
					<div class="flex-grow-1">
						<OptionMenu>
							<Items>
								<OptionItem Text="Details" OnClick="() => Detail(context)" />
								<OptionItem Text="Edit" OnClick="() => Edit(context)" />
								<OptionItem Text="Delete" OnClick="async () => await DeleteAsync(context)" />
							</Items>							
						</OptionMenu>
						<div class="mb-2">
							<TelerikFontIcon Icon="FontIcon.DocumentManager" ThemeColor="@ThemeConstants.SvgIcon.ThemeColor.Primary" />
							<strong class="ms-2">@context.Name</strong>
						</div>
					</div>
				</div>
				<hr />
			</Template>
		</TelerikListView>
	</Body>
</MxView>